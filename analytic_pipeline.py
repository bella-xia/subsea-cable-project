import subprocess

# define all scripts 
GEOLOC_FILTER_SCRIPT = 'geodata_filter.py'
PRELIMINARY_ANALYSIS_SCRIPT = 'preliminary_visual.py'



if __name == '__main__':

    # step 1: filter out data
    filtered_data = subprocess.run(
            ['python', ]
            )
